{% extends 'dashboard_base.html.twig' %}

{% block title %}Ouverture en Janvier 2024{% endblock %}

{% block body %}
    <section class="grid min-h-full place-items-center bg-white px-6 py-24 sm:py-32 lg:px-8">
        <div class="text-center">
            <div class="mt-20 lg:mt-0">
                <div class="inline-flex space-x-6">
                    <span class="rounded-full bg-main/10 px-3 py-1 text-sm font-semibold leading-6 text-indigo-600 ring-1 ring-inset ring-indigo-600/10">+400 annonces déjà enregistrées</span>
                </div>
            </div>
            <h1 class="mt-6 text-3xl font-bold tracking-tight sm:text-5xl">Ouverture en Janvier 2024</h1>
            <p class="mt-6 text-base leading-7 text-gray-600">Merci de votre inscription, nous revenons vers vous rapidement pour l'ouverture de la plateforme.</p>
            <div class="mt-10 flex items-center justify-center gap-x-6">
                <a href="{{ path('app_home') }}" class="rounded-md bg-main transition duration-200 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-main-strong">Revenir à l'accueil</a>
                <a href="{{ path('app_contact') }}" class="text-sm font-semibold text-gray-900">Contacter le support <span aria-hidden="true">&rarr;</span></a>
            </div>
        </div>
    </section>
{% endblock %}
